<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />

    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: Arial, sans-serif;
        }

        .logout-btn {
            background: transparent;
            border: none;
            color: purple;
            cursor: pointer;
            font-size: 16px;
        }

            .logout-btn:hover {
                color: darkred;
            }

        .sidebar {
            width: 250px;
            height: 100%;
            position: fixed;
            padding-top: 20px;
            box-shadow: 5px 0 15px rgba(0, 0, 0, 0.3);
            border-bottom-right-radius: 25px;
            border-top-right-radius: 25px;
            font-weight:bold;
        }

            .sidebar img {
                max-width: 60%;
                height: auto;
                margin-bottom: 19px;
                margin-left: 40px;
                margin-top: -20px;
            }

            .sidebar a.nav-link {
                display: block;
                color: black;
                padding: 15px;
                text-decoration: none;
                transition: background-color 0.3s ease;
            }

                .sidebar a.nav-link:hover {
                    background-color: purple;
                    color: white;
                    border-radius: 7px;
                }

        .header {
            position: fixed;
            top: 0;
            left: 250px;
            right: 0;
            height: 60px;
            background-color: transparent;
            color: purple;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 20px;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.3);
            z-index: 101;
        }

            .header h1 {
                margin: 0;
                font-size: 24px;
            }

        .content {
            margin-left: 250px;
            padding: 20px;
        }

        .active {
            background-color: purple;
            color: white !important;
            border-radius: 7px;
        }

        footer {
            margin-top: 50px;
        }

        .navbar-brand {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="sidebar">
        <div class="container">
            <!-- Replace this with your image source -->
            <img src="~/Image/03_01_INTELLIGENT-EXAM-MANAGEMENT-SYSTEM-284x300.png" alt="Logo" />
            <ul class="navbar-nav flex-grow-1">
                <li>@Html.ActionLink("Home", "Index", "StudentDashboard", new { area = "" }, new { @class = "nav-link" + (ViewBag.Title == "Student Dashboard" ? " active" : "") })</li>
                <li>@Html.ActionLink("Apply Exam", "Index", "Applycourse", new { area = "" }, new { @class = "nav-link" + (ViewBag.Title == "Apply Exam" ? " active" : "") })</li>
                <li>@Html.ActionLink("Attend Exam", "Appliedexam", "Applycourse", new { area = "" }, new { @class = "nav-link" + (ViewBag.Title == "Appliedexam" ? " active" : "") })</li>
                <li>@Html.ActionLink("My Result", "Myresult", "Applycourse", new { area = "" }, new { @class = "nav-link" + (ViewBag.Title == "Myresult" ? " active" : "") })</li>
                <li>@Html.ActionLink("Update details","Editdetails", "Applycourse", new { area = "" }, new { @class = "nav-link" + (ViewBag.Title == "Edit Details" ? " active" : "") })</li>



            </ul>
        </div>
    </nav>
    <div class="header">
        <h1>@ViewBag.Title</h1>
        <form action="@Url.Action("StudentLogin", "Login")" method="post" style="display:inline;">
            @Html.AntiForgeryToken()
            <button type="submit" class="logout-btn" title="Logout">
                <i class="fas fa-sign-out-alt"></i> Logout
            </button>
        </form>
    </div>

    <!-- Main Content Area -->
    <div class="content">
        @RenderBody()
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
